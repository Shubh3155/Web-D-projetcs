TODOs for react-dashboard:
- The project includes Tailwind config and PostCSS. Run `npm install` to install dependencies.
- Ensure `react-router-dom` is installed for routing. If not, run `npm install react-router-dom`.
- You can start dev with `npm run dev` after installing.
