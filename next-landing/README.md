# Next Landing

Install

```bash
npm install
npm run dev
```

This project includes Tailwind configuration in `tailwind.config.cjs` and PostCSS in `postcss.config.cjs`. Global CSS is in `styles/globals.css`.
